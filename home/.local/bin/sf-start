#!/usr/bin/env bash

set -e

cd ~/Projects/secureframe
make docker
tmuxit --session "workin-hard" \
    "yarn && make frontend" \
    "h50%" "make bundle && make db_migrate && make server" \
    "50%" "make worker"
